import { styled } from "styled-components/native";
import { ScrollView } from "react-native";

export const FeedStyled = styled(ScrollView)`
  width: 100%;
  display: flex;
  flex-direction: column;
`;
